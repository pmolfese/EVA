# EVA Developer Documentation

Maintainer-facing reference: where a feature lives and how to change it. Distinct
from `docs/manual/` (user-facing) and `docs/provenance/` (method specs).

Start here:

- **[architecture.md](architecture.md)** — the architecture map: module
  topology, the central data model, end-to-end data flow, one paragraph per
  subsystem, and the cross-cutting spines. *(DEV-1a — done.)*

- **[subsystems/](subsystems/README.md)** — one page per top-level source group:
  purpose, entry points, a one-line synopsis of every file, and a "how to extend
  this" note. *(DEV-1b — done.)*

Planned (see `ROADMAP.md` §12):

- **`features.md`** — a feature → code-and-entry-point index. *(DEV-1c)*
