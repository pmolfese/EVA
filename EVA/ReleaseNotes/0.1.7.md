---
version: 0.1.7
date: 2026-08-27
title: History (beta), cluster permutation statistics, automatic BCG detection, and a clean-room gradient engine
---

This release adds a beta undo/redo history to the waveform view, a Trials &
Statistics workspace for cluster permutation tests and per-trial diagnostics,
automatic detection of cardiac (BCG) components during ICA, a unified Channels
window, Finder QuickLook previews and thumbnails for MFF files, replaces EVA's
gradient/FASTR correction engine with a clean-room implementation, adds a set
of new figures for averaged (PSA) data plus a session-wide figure export
basket, and closes out a run of correctness fixes found while building all of
the above.

## History (beta): undo, redo, and where a file has been

The waveform view's Queue/History popover now tracks a real processing
history, not just a readout. This is the first release of History, and it
ships as **beta** — the mechanics below are solid, but the surface is new
enough that we're still watching for edge cases in unusual processing orders
before calling it done.

- **Click forward and back** through everything you've done to a recording —
  filter, ICA, artifact cleaning, wavelet reduction, channel marks,
  segmentation — restored instantly from an in-memory snapshot rather than
  re-run. ⌘Z / ⇧⌘Z drive the same back/forward transport, and name the step
  they act on ("Undo Filter") rather than reading as a bare, generic Undo.
- **Undo/redo is linear per window**, the way undo works everywhere else: a
  replaced step keeps its future reachable as Redo until you apply something
  different from an earlier point, at which point the abandoned future and its
  snapshots are discarded rather than kept around as a permanent branch.
  Re-applying the exact action you just undid reuses the retained node instead
  of creating a duplicate.
- **Fork to New Window** is the explicit way to keep two versions of a
  decision alive side by side — pick it from a history row and the new window
  gets its own copy of the lineage and snapshots, and the two windows evolve
  independently from there.
- **Opening an already-processed file now shows its real lineage.** A
  package's own `eva.xml` is read back on open and folded in as history: a
  filtered, cleaned, interpolated file no longer reads as "raw" just because
  this session hasn't touched it yet. Steps from before this session are shown
  but not clickable — there's no snapshot to restore into, only the fact that
  they happened — which reuses the same "can't get there instantly" treatment
  every other history node already has.
- **Average referencing and baseline correction are now their own steps**
  (`reference` and `baseline`, previously buried as checkboxes inside the
  filter and PSA panels), so each is its own node you can navigate to instead
  of a setting that silently changed the step that happened to host it.
  `reference` also now records which channels were excluded from the average,
  which used to be invisible to the history entirely.
- **Batch replay asks before carrying bad-channel decisions to a different
  file.** A script recorded on one recording used to apply its bad-channel
  list to every file in a batch, or ignore it outright, depending on the path.
  Both interactive and headless replay now pause on the same question — a
  sheet lists the carried channels, flags any this file cannot honor, and
  applies only what you keep.
- **The History tab shows what it's holding**, with a footer readout of cached
  snapshot memory against the budget, and rows can be pinned (exempt from
  eviction, up to a capped share of that budget) or renamed.

## Gradient correction: a clean-room engine

EVA's gradient artifact correction (average-artifact subtraction, FASTR-family
local templates, ANC) has been rewritten from a clean specification rather
than ported from any reference implementation, on both CPU and Metal.

Three findings came out of this work that the reference literature doesn't
address, and are now built in as safety constraints rather than left to the
user to notice:

- A template scale fitted per-epoch can absorb real brain signal that happens
  to correlate with the artifact over that short window — the default is now
  a median-smoothed fit rather than the raw one.
- Automatic OBS component removal can strip pure brain signal once template
  subtraction has already explained the artifact; a residual-energy floor now
  guards against that.
- ANC's adaptive step size needs a floor relative to the reference signal's
  own variance, not just an absolute one, or it can eat uncorrelated signal.

The previous FASTR implementation, its Metal backend, and its tests have been
removed.

## New: a Trials & Statistics workspace

The single-trial sheet gains two new tabs alongside Measurements, Woody
Alignment, RIDE, and CWT Ridge.

**Cluster Statistics** runs spatiotemporal cluster permutation tests directly
on retained pre-average epochs — a two-condition t-test or an omnibus
multi-condition F-test, each available in an independent-samples and a
repeated-measures form. The two exchangeability schemes genuinely differ:
independent designs shuffle condition labels across the whole pool, while
repeated-measures designs permute labels only *within* each paired unit
(subject or trial slot), which is what keeps the test sensitive to a
within-subject effect instead of drowning it in between-subject variance.
Pairing is by subject ID when a grand average carries one, or by trial order
when it doesn't — the UI says plainly which one applied, since the two are not
equally defensible and trial-order pairing is only meaningful when the
paradigm actually pairs trials by position.

**Trial Diagnostics** answers a different question: not "does condition A
differ from condition B," but "does this *particular trial* belong in its
condition's average at all." Every trial is scored against a leave-one-out
average of the others in its category — never against an average containing
itself, which would inflate the similarity of exactly the trials worth
scrutinizing — on both shape (correlation) and magnitude (regression slope),
since those fail differently: a trial can have the right shape at the wrong
size (the signature of a lapse in attention) or the right size in an inverted
polarity (a possible mislabeling). A trial is also checked against every
*other* category's average, so a labeling error shows up directly as "this
trial looks more like condition B than its own condition."

A reviewed exclusion — trials you flagged, or a flagged trial you put back —
can now be **committed**, re-averaging immediately and recording a real
`trialExclusion` step alongside the thresholds that proposed it, so the
decision replays exactly rather than being re-derived from criteria that could
propose a different set after any upstream change. Excluded trials stay in the
data and stay visible (flagged, not removed) in Single Trial Analysis; the
step shows up in history, QuickLook retention bars, and the export audit log.
Copy a script to another subject and the trial list doesn't cross over with
it — EVA notices the recorded trials don't resolve on that file and re-opens
the thresholds for review instead of silently applying someone else's
decisions. Clearing a commit offers "this category" versus "all categories"
only when that distinction is real; with one committed category the two are
the same button.

The workspace lays out as a dashboard: a persistent left rail with the
category summary, a shape-vs-magnitude scatter, and exclusion criteria you can
drag while watching which trials respond, beside a tabbed pane — Windows,
Drift, Stability, Trials, Effect — sized to show real detail rather than a
column of small charts. You can drop one or more named analysis windows
directly onto the trial-average plot (drag to place, drag the edges to
resize) to score a single component instead of the whole epoch, and any
proposed exclusion previews its effect on the resulting average's SNR against
a random-exclusion baseline, so an improvement is never mistaken for proof
that the excluded trials were actually bad — selecting trials by similarity to
an average always improves that average's own similarity score, which is
exactly why the comparison is there.

## New: automatic BCG component detection

ICA component review now offers a scanner-specific suggestion for which
components are ballistocardiogram (BCG) artifact, alongside the existing
ICLabel-style heuristic. The labeller looks at beat-locked energy, how
consistently a component's waveform matches an averaged beat template,
whether that template's peak follows the R-wave (rather than coincidentally
preceding it), and — when an ECG channel is available — the component's
correlation with the ECG at physiologically plausible delays. R-wave
detection and the ECG review view both received matching improvements, so the
beat markers the labeller relies on are themselves more reliable.

## New: a unified Channels window

Channel Sets, Health, Impedance, Relationships, and Reference are now tabs of
one Channels window rather than separate panels, with a scalp map that stays
in the same place while the sidebar and inspector change meaning by tab —
only the Channel Sets tab can edit set membership; every other tab uses map
clicks purely for inspection.

The new **Relationships** tab surfaces channel-pair diagnostics that were
previously invisible: likely electrode bridges and unusually high
inter-channel correlation, reported as explainable findings rather than an
automatic bad-channel decision — a persistent near-identical relationship
between two channels is something to look at, not something EVA should
silently act on.

## New: Finder previews for MFF files

`.mff` packages now get a QuickLook preview and Finder thumbnail without
opening EVA. The preview panel reads only the small XML sidecars and the
signal file's first block header — never the sample data itself, so it stays
fast even on a multi-gigabyte recording — and shows the file's continuous,
segmented, or averaged/grand-average status, channel count, sampling rate,
duration, an event timeline or per-condition segment retention (kept vs.
rejected, when the package recorded it), electrode impedance colored by
EVA's own good/fair/poor bands, and whether EVA has processed the file at
all. Segmentation now records, per category, how many candidate trials it
started with and why any were dropped — labeled bad, an artifact, out of
bounds, too many bad channels — so that retention story survives being
written to disk and read back later, including by the QuickLook preview.

## New: figures for averaged data

Several new views for looking at PSA/averaged conditions, each reusing the
existing plot renderers rather than drawing anything new:

- **Multi-Butterfly** — one all-channel butterfly plot per selected condition,
  stacked vertically, instead of one plot with every condition overlaid.
- **Filmstrip** — evenly-spaced topomaps across the whole epoch, one row per
  condition, for a fast first look before you know where the peaks are.
- **Joint markers** — an MNE `plot_joint`-style time marker with a linked,
  draggable topomap box, addable to any butterfly pane by right-click.
- **Difference waves** — condition A − condition B as a proper synthetic
  condition, so the existing butterfly, GFP, and topomap views draw it exactly
  as if it were recorded.
- **GFP strips** — Global Field Power (the standard "how much is happening,
  independent of location" summary) drawn under a butterfly plot.
- **Regex pattern helper** — a library of common patterns plus a live tester
  for the PSA regex sub-selection popover, so writing a category rule doesn't
  require already knowing regex.
- **Real axes on exported figures** — exported butterfly-style PNGs/PDFs now
  draw tick marks and labels into the image, since a static export has no
  toolbar to supply that context the way the live view does.

## New: a session-wide figure export basket

"Add to Export" on any figure's right-click menu now adds it to a persistent
basket (Window ▸ Figure Export) rather than saving it immediately. Each figure
is captured as a vector PDF and a full-quality PNG at add-time, so the basket
has no dependency on the source recording staying open, and can be reordered
and exported either as one multi-page contact sheet or as **individual PDF or
PNG files**, named `<prefix>-<N>` with `N` matching the figure's numbered
position in the basket list.

## Display scale in physical units

The waveform toolbar's Scale and Time Scale sliders now show what they mean in
the units the rest of EEG software uses — µV/mm and mm/s — next to the raw
numbers, with a popover for typing an exact value and a one-click "Clinical"
preset (7 µV/mm, 30 mm/s). Millimetres are nominal (72 points/inch) since EVA
doesn't measure the display; exported figures state the same numbers exactly,
since a PDF point is a physical unit by definition. Settings now has a default
sensitivity/sweep speed applied to newly-opened recordings.

Fixed along the way: the sweep speed silently varied by up to 25% between
files at different sampling rates, because the on-screen decimation stride is
an integer and only lands exactly on target for rates that are multiples of
200 Hz. The physical-units conversion now accounts for this per file; a
genuine fix for the underlying decimation is still open.

## Also new

- **Elliptic (Cauer) filters** join the existing Butterworth option for IIR
  high-pass and low-pass edges, for the steeper rolloff an elliptic design
  gives at the cost of passband ripple.
- **Event-timing preferences** (Preferences ▸ Events) let you tell EVA that a
  particular event code marks the *center* of what it describes rather than
  its onset — a lab that stamps one blink marker at the apex, for example.
  Every window derived from that marker (cleaning, epoch previews, the
  waveform highlight band) uses the corrected anchor instead of silently
  running half a duration late. The tab lists the event codes actually
  present in your open recordings, so a rule is added by picking a code
  rather than typing one blind.
- **Cleaning variance accounting** gives gradient correction, ICA component
  removal, CWL, wavelet reduction, and the artifact cleaner a uniform answer
  to "how much did this stage actually remove," reported as a fraction of the
  input signal's own variance and logged in the order the stages actually
  ran — useful for noticing that one stage in a pipeline is doing unusually
  little or unusually much, though it is a variance measure, not a quality
  score, and an aggressive stage on a genuinely noisy channel is expected to
  score high.
- **Sensor names from `sensorLayout.xml`** are read and used wherever a
  channel name is shown, instead of falling back to a bare index everywhere
  the file didn't otherwise supply one.
- **The Averages workspace's Topography pane is now resizable** by dragging
  its left edge, and its topomap grid wraps to as many columns as fit rather
  than a fixed two. The view-toggle toolbar adapts to available width instead
  of forcing the window wider than the screen. Topomap rendering itself moved
  from a per-frame calculation to a cached image that is scaled rather than
  recomputed on resize, which is what makes the drag itself smooth.

## Fixes

- **App menu** Command-Q had stopped working after opening Settings; it now
  lives in the App menu and remains available.
- **App lifecycle** Closing the final empty window now quits EVA, completing
  the intended close-recording → close-window flow.
- **History** Rewinding now clears processing-dependent state and stale segment
  health results that belong to later steps.
- **File opening** Double-clicking a file in Finder no longer occasionally
  creates two windows; the main window is now single-instance.
- **History** Fork to New Window now works for BrainVision and other
  non-MFF imports; it previously failed to load in the new window because the
  security-scoped access to the recording's folder wasn't carried over.
- **PSA provenance** Regex-only category averages are now written to the
  replayable `eva.xml` script as well as the audit log.
- **Figure export** PNG and JPEG exports now declare their resolution, avoiding
  double-size placement in other applications.
- **Channel health** Per-channel analysis now runs concurrently.
- **Filtering** IIR slope labels now report the effective forward-backward
  attenuation rather than the one-pass design slope.
- **Artifact cleaning** MAR and wAAR now fit template amplitude with the correct
  least-squares denominator, preventing scaled artifacts from being amplified.
- **Artifact detection** Waveform matching now always checks a dense candidate
  grid, with finer spacing, so unrelated large peaks cannot hide valid matches.
- **Artifact detection** Ocular-threshold and continuous-topography runs are now
  merged across brief gaps before duration checks, while respecting maximum event
  durations so chains of blinks or eye movements are not discarded.
- **Wavelets** A flat denoised result now reports zero correlation instead of a
  misleading perfect correlation.
- **Wavelets** SURE thresholding now uses the correct SureShrink fallback
  criterion, avoiding unintended over-cleaning.
- **Gradient correction** OBS candidate limiting no longer divides by zero when
  asked to retain a single candidate.
- **Signal analysis** ICA fitting, waveform matching, and scalograms now use
  anti-aliased sinc downsampling instead of raw sample skipping.
- **Cardiac detection** Spatial PCA now uses an exact eigensolver, so zero-sum
  and other dipolar BCG patterns cannot be missed because of an unlucky seed.
- **Cardiac detection** Global field power now removes the instantaneous spatial
  mean, preventing common-mode activity from appearing as BCG.
- **Trial alignment** MAP latency alignment now compares mean error over adequate
  overlap, removing its bias toward large boundary shifts.
- **Woody alignment** Correlations now use equal sample support at every lag, and
  padded epoch edges no longer attenuate adaptive templates or aligned averages.
- **Woody diagnostics** Shift median, variability, correlation support, and
  search-limit hits now make unstable or boundary-limited alignments visible.
- **RIDE decomposition** Component updates now use robust pointwise medians and
  are refit at the final latency solution before reconstruction.
- **RIDE locking** Response windows are now response-relative, per-trial locking
  requires real response markers, and fixed-marker limitations are reported.
- **RIDE diagnostics** C latency and adjustment are reported separately alongside
  residual RMS, explained variance, component energy, and convergence warnings.
- **Epoch metrics** Plus-minus noise now balances odd trial counts so an
  unmatched trial cannot leak ERP signal into the noise estimate.
- **Epoch metrics** GFP SNR now uses spatial standard deviation rather than RMS
  voltage, making it independent of common-mode activity.
- **Segment health** GFP baselines and burst scores now use spatial standard
  deviation rather than reference-dependent RMS voltage.
- **Connectivity** Band PLV is now computed per frequency bin before averaging,
  so genuine coupling cannot cancel across frequencies.
- **Recording combine** Grand-average bad-channel policies now restore saved
  decisions and either exclude or spherical-spline interpolate bad channels as
  selected, with per-file provenance and explicit errors when interpolation is
  unavailable.
- **Recording combine** Inputs are now matched by unique channel identity and
  reordered when safe; incompatible layouts, rates, or failed loads block the
  operation instead of silently mixing data.
- **File loading** EDF+ annotations are decoded, declared BrainVision marker
  failures are reported, and MFF event labels, descriptions, cells, and durations
  survive export and reimport.
- **Recording combine** Append and grand-average results now rebuild duration,
  events, epoch segments, and structural flags for their new timeline instead of
  inheriting stale metadata from the first input.
- **File loading** EDF, Persyst, and BESA readers now reject truncated, ragged,
  non-finite, and invalidly calibrated inputs; signed Persyst 32-bit data and
  supported binary/text orientations are decoded with validated scaling.

## Documentation

A [documentation site](https://pmolfese.github.io/EVA/) has been added,
covering supported formats, core processing concepts, and a contributor guide
for keeping the manual in sync with the app.
